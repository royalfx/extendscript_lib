/**
 * as_isEffect.jsxinc
 * version: 1.0.2
 * author: Alexander Semeniuk
 * date: Jul 16 2019
 * 
 * @param {prop} Target property
 * @returns {boolean} ...
 */
function as_isEffect(prop) {
	if(!isValid(prop) || !prop) {
		return false;
	}
	return (prop.parentProperty && (prop.parentProperty.matchName == "ADBE Effect Parade"));
}