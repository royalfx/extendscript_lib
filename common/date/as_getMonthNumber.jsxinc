/**
 * @description This format allows you to sort() months
 * @param {Date} date
 * @returns {number} month num
 */
function as_getMonthNumber(date) {
    return (date.getFullYear() - 1970) * 12 + date.getMonth() + 1;
}
